递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[智能手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=11 的后代分类[iPhone],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[服装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[T恤],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=3 的后代分类[跑鞋],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=6 的后代分类[长裙],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=4 的后代分类[厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=4 的后代分类[厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=4 的后代分类[厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=4 的后代分类[厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=4 的后代分类[厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=4 的后代分类[厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=4 的后代分类[厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=15 的后代分类[POLO衫],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=4 的后代分类[厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=15 的后代分类[POLO衫],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=4 的后代分类[厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=15 的后代分类[POLO衫],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=7 的后代分类[奢侈品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=4 的后代分类[厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=15 的后代分类[POLO衫],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=7 的后代分类[奢侈品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=4 的后代分类[厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=15 的后代分类[POLO衫],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=7 的后代分类[奢侈品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=4 的后代分类[厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=15 的后代分类[POLO衫],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=7 的后代分类[奢侈品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=4 的后代分类[厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=15 的后代分类[POLO衫],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=7 的后代分类[奢侈品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=4 的后代分类[厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=15 的后代分类[POLO衫],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=7 的后代分类[奢侈品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=4 的后代分类[厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=15 的后代分类[POLO衫],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=7 的后代分类[奢侈品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=4 的后代分类[厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=15 的后代分类[POLO衫],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=7 的后代分类[奢侈品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=4 的后代分类[厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=15 的后代分类[POLO衫],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=7 的后代分类[奢侈品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=4 的后代分类[厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=15 的后代分类[POLO衫],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=7 的后代分类[奢侈品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=4 的后代分类[厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=15 的后代分类[POLO衫],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=7 的后代分类[奢侈品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=4 的后代分类[厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=15 的后代分类[POLO衫],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=7 的后代分类[奢侈品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=4 的后代分类[厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=15 的后代分类[POLO衫],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=7 的后代分类[奢侈品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=4 的后代分类[厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=15 的后代分类[POLO衫],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=7 的后代分类[奢侈品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=4 的后代分类[厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=15 的后代分类[POLO衫],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=7 的后代分类[奢侈品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家用电器],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=1 的后代分类[电视],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[手机/运营商],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=2 的后代分类[手机],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[电脑/办公],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[家居/家居/家装/厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=4 的后代分类[厨具],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男装/女装/童装/内衣],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[男装],并将其作为当前节点,查找子分类,本轮遍历挂起)
----递归调用之前(找到了pid=15 的后代分类[POLO衫],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=5 的后代分类[女装],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[美妆个护/宠物],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[女鞋/箱包/珠宝],并将其作为当前节点,查找子分类,本轮遍历挂起)
--递归调用之前(找到了pid=7 的后代分类[奢侈品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[男鞋/运动/户外],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[汽车/汽车用品],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[食品/酒类],并将其作为当前节点,查找子分类,本轮遍历挂起)
递归调用之前(找到了pid=0 的后代分类[图书],并将其作为当前节点,查找子分类,本轮遍历挂起)
